#!/usr/bin/env python


import os
import sys

# Add the parent directory to Python path so checkm2 module can be found
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from checkm2.main import main

if __name__ == '__main__':
    main()